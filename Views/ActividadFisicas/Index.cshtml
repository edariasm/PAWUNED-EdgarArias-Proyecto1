@model IEnumerable<PAWUNED_EdgarArias_Proyecto1.Models.ActividadFisica>

@{
    ViewData["Title"] = "Index";
}
<link href="/css/Styles.css" rel="stylesheet" type="text/css">

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoActividad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaHoraActividad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DuracionMinutos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DistanciaRecorrida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsumoCalorico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Comentarios)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoActividad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHoraActividad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DuracionMinutos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DistanciaRecorrida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConsumoCalorico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comentarios)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioNavigation.IdUsuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRegActividad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRegActividad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRegActividad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
